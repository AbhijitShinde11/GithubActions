name: Output Information
on: workflow_dispatch
jobs:
    output:
        runs-on: ubuntu-latest
        steps:
            - name: Github context
              run: echo "${{toJSON(github)}}" # output the GitHub context information